<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_enabled="true" android:state_pressed="false">
        <shape xmlns:android="http://schemas.android.com/apk/res/android">

            <!-- Background Color -->
            <solid android:color="@color/colorAccent" />
            <corners android:radius="10dp" />

            <!-- Border Color -->
            <stroke android:width="1dp" android:color="@color/colorAccent" />

            <padding android:bottom="5dp" android:left="10dp" android:right="10dp" android:top="5dp" />

        </shape>
    </item>

    <item android:state_enabled="true" android:state_pressed="true">
        <shape xmlns:android="http://schemas.android.com/apk/res/android">

            <!-- Background Color -->
            <solid android:color="@color/light_button_color" />
            <corners android:radius="10dp" />
            <!-- Border Color -->
            <stroke android:width="1dp" android:color="@color/light_button_color" />

            <padding android:bottom="5dp" android:left="10dp" android:right="10dp" android:top="5dp" />

        </shape>
    </item>

    <item android:state_enabled="false">
        <shape xmlns:android="http://schemas.android.com/apk/res/android">

            <!-- Background Color -->
            <solid android:color="@color/very_light_button_color" />
            <corners android:radius="10dp" />
            <!-- Border Color -->
            <stroke android:width="1dp" android:color="@color/very_light_button_color" />

            <padding android:bottom="5dp" android:left="10dp" android:right="10dp" android:top="5dp" />

        </shape>
    </item>
</selector>